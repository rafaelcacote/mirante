/**
 * Serviço de Eventos (BuySystem API)
 *
 * Use preferencialmente `apidogService` de apidog.js para operações de eventos.
 * Este arquivo é mantido para compatibilidade com imports existentes.
 */

export { apidogService as eventsService, default } from './apidog.js'
